---
- hosts: all
  vars:
    ansible_python_interpreter: "/usr/local/bin/python"
  become: yes
  gather_facts: yes


  tasks:
  - name: minimum packages
    pkgng: name={{item}} state=present
    with_items:
    - rsync
    - ezjail
    - vim-console

